# dependencies
node_modules/

# build / wrangler
dist/
.wrangler/
.mf/

# local secrets (Workers convention) — NEVER commit
.dev.vars
.dev.vars.local
!.dev.vars.example
.env
.env*.local

# keys / deploy credentials (never commit)
*.pem
.keys/
*.cf-token
.eveoy-cf-deploy.env

# testing
/coverage

# tdd-guard — local enforcement state + machine-specific hook (NEVER commit)
.claude/tdd-guard/
.claude/settings.local.json

# misc
.DS_Store
*.tsbuildinfo

# editor
.vscode/
.idea/

# logs
*-debug.log*
npm-debug.log*
